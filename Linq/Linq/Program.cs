using System;
using System.Collections.Generic;
using System.Linq;

namespace Linq
{
    class Program
    {
        static void Main(string[] args)
        {
            //var marks = new List<int>() { 95,93,80,90,77 };
            //foreach(var item in marks)
            //{
            //    Console.WriteLine(item);
            //}
            //LINQ Query syntex
            //var result = (from m in marks
            //             where m >= 80
            //             select m).Sum();
            //var result = (from m in marks
            //              where m > 70 && m<90
            //              select m).Sum();
            //var result = (from m in marks
            //              where m > 70 && m < 90
            //              select m).Average();
            //var result = (from m in marks
            //              where m > 70 && m < 90
            //              select m).Max();
            //var result = (from m in marks
            //              where m > 70 && m < 90
            //              select m).Max();
            //var result = (from m in marks
            //              where m > 70 && m < 90
            //              select m).OrderBy(m=> m);
            //Console.WriteLine(result);
            //foreach(var item in result)
            //{
            //    Console.WriteLine(item);
            //}

            //LINQ method syntex
            //var result1 = marks.Where(x => x >= 80).Sum();
            //var result1 = marks.Where(x => x > 70 && x < 90).Sum();
            //var result1 = marks.Where(x => x >70 && x < 90).Average();
            //var result1 = marks.Where(x => x >70 && x < 90).Max();
            //var result1 = marks.Where(x => x >70 && x < 90).OrderBy(x=>x).First();
            // var result1 = marks.Where(x => x >70 && x < 90).Min();
            //Console.WriteLine(result1);
            //foreach (var item in result1)
            //{
            //    Console.WriteLine(item);
            //}
            //var names = new List<string>() { "Mashrafi", "Shakib", "Mushfiqe", "Mahmudulla" };
            //var result = names.Where(x => x.ToLower().StartsWith("maS".ToLower()));
            //foreach (var item in result)
            //{
            //    Console.WriteLine(item);
            //}
            //var result = names.OrderBy(x => x).Aggregate((s1, s2) => s1 + "," + s2);
            //Console.WriteLine(result);           
      
        }
    }
}
